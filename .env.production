# Production environment
# Direct backend URL — used by the built bundle (no proxy in prod)
VITE_API_URL=http://ec2-13-60-234-74.eu-north-1.compute.amazonaws.com:8080

# Health check path used by /api/ping
VITE_HEALTH_PATH=/api/v1/actuator/health

